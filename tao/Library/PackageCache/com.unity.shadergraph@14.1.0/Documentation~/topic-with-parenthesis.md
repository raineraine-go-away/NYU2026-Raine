# Topic (with parenthesis)

To support the same table of contents style that the core manual uses, the DocTools package adds the `.md` extension to any Markdown link in the toc that doesn't end with `.md`. To allow you to use parenthesis in the Markdown link text, this behavior should only catch Markdown links and not just any text in parenthesis.